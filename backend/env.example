# Optional: set a Groq API key for real LLM generation
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.1-8b-instant

# Optional: force Groq (disable the local fallback generator)
# REQUIRE_GROQ_API_KEY=true

# Optional: override database (default is local SQLite at backend/wiki_quiz.db)
# DATABASE_URL=sqlite:///./wiki_quiz.db



